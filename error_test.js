// Тестовый файл для проверки нейтрализации ошибок методом Айронса

// Отсутствующий идентификатор 
let = { "key": 42 };

// Отсутствующий оператор присваивания
let testVar { "name": 1 };

// Непарные скобки
let testVar2 = { "age": 25, };

// Отсутствующее двоеточие
let testVar3 = { "height" 180 };

// Отсутствующая точка с запятой
let testVar4 = { "weight": 70 }

// Некорректный идентификатор
let @invalidVar = { "error": 404 };

// Неправильная последовательность ключей и значений
let testVar5 = { "key1": 1, "key2" 2, "key3": 3 };

// Пропущенная запятая
let testVar6 = { "a": 1 "b": 2 };

// Корректное объявление для сравнения
let correctVar = { "correct": 100 };

// Несколько ошибок подряд
let errorTest = { 
  "missing-colon" 500,
  @invalid-key: 600,
  "unclosed-string: 700
}; 